/**
 * Supabase client export
 * 
 * Re-exports supabase client from supabaseclient.ts for backward compatibility
 * with existing imports throughout the application.
 * 
 * @fileoverview Supabase client utilities
 */

export { supabase } from "./supabaseclient";
